# These files will be ignored by GitHub when it computes programming language statistics for this repository
tests/**/*.html linguist-vendored
tests/**/*.json linguist-vendored

# These files will be normalized on commit (CRLF converted to LF) - asterisk is a wildcard
* text eol=lf

# These files will NOT be normalized
*.png -text
*.jpg -text
*.gif -text
*.ico -text
